import Button from "./Button"

const Header = () => {
    return (
        <header className="header">
            <h1 textColor>Task Tracker</h1>
            <Button color="#05AFF2" text="Adicionar"/>
        </header>
    )
}

export default Header
